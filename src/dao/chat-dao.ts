

class ChatDAO {
    // async sendMessage(sender: any, receiverId: any, message: any) {
    //   return await Chat.create({ sender, receiverId, message });
    // }
  
    // async getMessagesForUser(userId: any) {
    //   return await Chat.find({ receiverId: userId }).sort({ timestamp: 1 });
    // }
  }
  